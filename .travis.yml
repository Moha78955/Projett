language: java
install: skip
jdk: oraclejdk11
before_install:
  - chmod +x gradlew
script:
  - ./gradlew build 

deploy:
  provider: releases
  user: "Moha78955"
  password: "Mo0210@m"
  skip_cleanup: true
  on:
    tags: true

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/